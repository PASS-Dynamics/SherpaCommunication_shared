syntax = "proto3";

option csharp_namespace = "sherpa.Shared.Protos";

package sherpa.shared.protos;

message SearchLight {
  bool power = 1;
  int32 intensity = 2;
  bool beamAperture = 3;
}

service SearchLightService {
	rpc Communicate (stream SearchLight) returns (stream SearchLight);
}

